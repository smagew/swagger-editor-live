## About

UI for swagger file editing. 

## Usage


```bash
$ docker run -ti --rm -v ${PWD}:src -p 8000:8000 swagger-editor:latest
```


where `${PWD}` is your project root where swagger.json located
